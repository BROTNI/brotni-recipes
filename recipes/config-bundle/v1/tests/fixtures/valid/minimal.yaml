apiVersion: recipe.brotni.com/v1
kind: ConfigBundleRecipe

metadata:
  name: minimal-valid
  description: Minimal valid config-bundle recipe — only required fields.

configuration:
  format: json
  data: |
    { "feature_flags": { "new_router": true } }
